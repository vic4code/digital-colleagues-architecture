window.IMPLEMENTATION_DOCUMENTS = {"reading/research/source-notes/openclaw.html": "<h1 id=\"openclaw-source-review-2026-09-08\">OpenClaw source review — 2026-09-08</h1>\n<p>Pinned revision: <code>91ea838947d30a65f1299b05fa42071917f2a293</code> (2026-09-08T07:23:11Z). Local full source: <code>/tmp/proactive-agent-research-20260908/source/openclaw/openclaw-91ea838947d30a65f1299b05fa42071917f2a293</code>.\nGitHub link prefix for every reference below: <code>https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/</code> + path + <code>#L&lt;n&gt;</code>.</p>\n<h2 id=\"main-corrections-to-documentation-only-conclusions\">Main corrections to documentation-only conclusions</h2>\n<ol>\n<li><code>create_goal</code> explicit-user/system requirement and <code>blocked</code> only after three matching blocker turns are <strong>model instructions</strong>, not provenance-checking / blocker-counting code. The state transition enforces nonempty objective, one existing goal, terminal state and token accounting. Do not label the semantic requirements hard security gates.</li>\n<li>Durable Goal is not itself a repeat-until-done scheduler. Start/resume creates a continuation input; active goal is injected into subsequent inbound turns. Full <code>src</code> search found <code>continuationTurns</code> only initialized to zero in <code>goals-transitions.ts:105</code> (excluding fixtures/tests), no increment/automatic continuation controller under that field. No goal-related invocation in scanned agent-runner finalization surfaces. This is bounded negative evidence, not proof every harness/extension lacks independent continuation.</li>\n<li>OpenClaw has an additional human-inspired <strong>dreaming</strong> memory mechanism. Its diary can form associations among existing fragments, but the traced diary path produces text, not a goal/task or environment-exploration action. Do not omit this mechanism; do not mistake poetic surprise language for intrinsic-reward computation.</li>\n</ol>\n<h2 id=\"1-heartbeat-actual-caller-chain-and-admission\">1. Heartbeat: actual caller chain and admission</h2>\n<p><code>src/cron/service/timer-execution.ts:161-216</code> handles heartbeat payloads and migrated heartbeat tasks. Constructs <code>source: interval</code>, <code>intent: scheduled</code> (or <code>task</code>), resolves target agent and calls <code>state.deps.requestHeartbeatAndWait</code> at 198. It converts settled heartbeat status to cron ok/error/skipped.</p>\n<p><code>src/infra/heartbeat-wake.ts:12-20</code> aliases this API to the shared session-event wake bus. <code>src/infra/session-event-wake.ts:498</code> enqueues an awaited wake; <code>:380-404</code> owns timer/handler dispatch, <code>:322-346</code> invokes the current handler under abort/owner-generation protection. <code>heartbeat-wake.ts:29-30</code> installs the handler.</p>\n<p><code>src/infra/heartbeat-runner-scheduler.ts:startHeartbeatRunner:48</code> registers <code>run</code> at 394. The targeted/broadcast path calls <code>runOneAgent</code> (:232), checks <code>evaluateWakeDeferral</code> (:239), then <code>runOnce</code> (:269). Retryable busy skips do not acquire cooldown (:300); accepted/failed attempts update per-agent bookkeeping. Targeted unscheduled wakes are allowed through an explicit branch (:323-348) without enrolling the agent in recurring scheduling.</p>\n<p><code>src/infra/heartbeat-cooldown.ts:93-154</code> implements intent-based admission. Explicit manual intent bypasses deferral (:94). Constants are minimum 30 seconds (:20), flood window 60 seconds (:26), five-start threshold (:27); ordinary event/task policies differ from authoritative scheduled ticks. These are code gates, not prompt requests.</p>\n<p><code>src/infra/heartbeat-runner-run.ts:29-36</code> calls <code>resolveHeartbeatWakeStage</code> then <code>prepareHeartbeatRunStage</code>. <code>heartbeat-runner-execution.ts:124-167</code> validates configured agent, global wake enablement, interval/targeted exception, and active hours (cron transport has its own exception). Busy checks occur at :191, :217, :233, :274, :290, :304, :312; route rejection at :367. This is not simply timer → LLM unconditionally.</p>\n<p>Once admitted, <code>heartbeat-runner-run.ts:73-121</code> constructs the internal prompt and source metadata then calls normal inbound dispatch; marks <code>isHeartbeat</code>, carries abort/timeout and optional lightweight context, and routes delivery through <code>deliverHeartbeatDispatch</code>. Model judgment occurs after this runtime boundary.</p>\n<p><strong>Design implication:</strong> scheduler supplies opportunities; runtime controls admission; model decides useful action from supplied state/prompt. Turning off recurring cadence is not equivalent to disallowing all event-driven turns.</p>\n<h2 id=\"2-adaptive-next_check-complete-proposal-propagation\">2. Adaptive next_check: complete proposal propagation</h2>\n<p><code>src/cron/isolated-agent/run.ts:213-214</code> registers the current job with <code>pacingEnabled</code> in run context. <code>src/agents/tools/cron-tool.ts:649-665</code> accepts <code>next_check</code> only with scoped jobId and runId, parses positive duration, calls registry.</p>\n<p><code>src/infra/agent-run-registry.ts:332-341</code> requires matching currently running job and pacing enabled, then stores the latest proposal; <code>:345-356</code> consumes/deletes it. This is a real runtime-scoped capability check.</p>\n<p>After <code>executeCronRun</code> and <code>finalizeCronRun</code>, <code>src/cron/isolated-agent/run.ts:297-304</code> consumes the proposal; attaches it only if status is ok, and discards it on exceptions. <code>src/cron/service/timer-execution.ts:437</code> propagates <code>nextCheck</code> to scheduler outcome.</p>\n<p><code>src/cron/service/timer-outcomes.ts:481-510</code> applies it only for enabled, successful, paced jobs; <code>src/cron/pacing.ts:39-54</code> clamps delay to declared min/max, based on run <strong>end</strong> time. Trigger safety floor outranks pacing bound at :500-504. Persists a <code>pacedNextRunAtMs</code> marker; outcome writes pass through <code>timer-outcome-finalization.ts:240</code> to <code>applyOutcomeToStoredJob</code> (:647). Failed jobs take normal error backoff (:434 onward), not the model proposal.</p>\n<p><strong>Prompt versus code:</strong> cron tool description <code>cron-tool.ts:203,210</code> explicitly teaches self-wakeup / continue-later / adaptive polling. The model chooses its desired delay; code checks provenance, bounds and successful execution. This is self-adjusted attention timing, not autonomous goal generation.</p>\n<p>Tests read, not executed: <code>src/agents/tools/cron-tool.pacing.test.ts:31-102</code> checks scoped proposal, single consumption, no-pacing rejection, other-job rejection, out-of-run rejection, shared-run separation. <code>src/cron/service/timer.pacing.test.ts:355,370</code> checks trigger floor and failed-run discard.</p>\n<h2 id=\"3-goal-persistent-objective-and-continuation-semantics\">3. Goal: persistent objective and continuation semantics</h2>\n<p><code>src/agents/tools/goal-tools.ts:95-117</code>: tool description says explicit request only; execute reads objective/budget and calls <code>createSessionGoal</code>. No explicit-request evidence parameter/check is present there.</p>\n<p><code>src/config/sessions/goals.ts:147-169</code> updates persistent session goal using <code>buildCreatedSessionGoal</code>; <code>goals-transitions.ts:78-106</code> checks nonempty/no-existing, assigns UUID, status active, baseline and continuationTurns=0. <code>:37-75</code> accounts fresh token usage and transitions active → budget_limited. <code>:109-164</code> rejects changing completed goals; resume of limited goals resets budget baseline.</p>\n<p><code>goal-tools.ts:122-141</code> exposes only complete/blocked status updates; the three-blocked-turn rule is tool description, while <code>buildUpdatedSessionGoalStatus</code> has no recurrence counter. These distinctions matter for governance.</p>\n<p><code>src/auto-reply/reply/commands-goal.ts:171,191</code> returns continuation prompts for start/resume; <code>:242-244</code> rewrites command input and returns goalContinuation into the ordinary reply path. <code>src/gateway/server-methods/sessions-goal.ts:104-125</code> routes UI resume through <code>handleSessionGoalResumeChat</code> in chat-send-handler. <code>src/auto-reply/reply/inbound-meta.ts:25-42</code> only injects active goal, bounded to 200 characters, into incoming context; paused/blocked/limited/complete goals are omitted. No independent scheduler is introduced by these functions.</p>\n<p><strong>Conclusion:</strong> goal keeps target sticky. It does not demonstrate an open-ended agent that invents its own goals, nor alone guarantee automatic next-turn continuation after a final answer.</p>\n<h2 id=\"4-workshop-event-triggered-autonomous-learning-chain\">4. Workshop: event-triggered autonomous learning chain</h2>\n<p><code>src/agents/embedded-agent-runner/run/attempt-finalize.ts:321-365</code> emits agent-end side effects for eligible non-detached attempts. Detached maintenance avoids agent_end so it cannot recursively generate successor review work. It captures a completed transcript-entry anchor, actual model facts and available Workshop tool.</p>\n<p><code>src/agents/harness/agent-end-side-effects.ts:38-59</code> reads the exact active anchor and calls <code>scheduleSkillExperienceReview</code>; <code>experience-review-default.ts:6-25</code> supplies system-active check and rereads current config before preparing/reviewing the candidate.</p>\n<p><code>src/skills/workshop/experience-review-scheduler.ts:14-24</code> sets 10 model iterations, 30-second idle window, max 32 pending candidates, excluded trigger/session categories. <code>isEligibleContext:85-107</code> requires resolved model, actual Workshop availability, no compaction, appropriate foreground session. <code>schedule:159-259</code> excludes provider errors/off mode/shallow turns/missing source; subsequent work resets quiet timer. <code>arm:114-153</code> validates generation, defers while active or review already in flight, deletes pending before one run, records failure rather than indefinite retry. This is actual timer/state-machine behavior.</p>\n<p><code>experience-review.ts:105-114</code> independently admits detached root work. <code>:122-140</code> chooses off/auto/propose and private session, auto execution root at Workshop directory, propose mutation budget 1. <code>:184-205</code> fences deleted/replaced source session, changed permission mode, stale transcript anchor and auto mode revocation. <code>:225-258</code> invokes <code>runSkillWorkshopReview</code>, strips foreground delivery capability, inherits permission mode, restricts tools, uses captured provider/model and configured timeout. <code>:276-294</code> distinguishes completed/proposed/nothing; completed means successful review execution, not guaranteed file change.</p>\n<p><strong>Meaning:</strong> the system initiates reflection without a new user prompt, grounded in previous work, and can choose what procedure to improve. It is procedural artifact learning, not parameter learning or self-generated external missions. Numeric two-roundtrip usefulness criteria in review prompt are model-evaluated heuristics, not measured benefit enforcement.</p>\n<p>Test read: <code>experience-review.test.ts:143-166</code> fake timers assert no review at 29,999 ms and one review after 30,000 ms; :237 defers while active; :486 drops failed review; :511 excludes ineligible runs. No node_modules/vitest exists in fresh archive, so no upstream tests executed; no dependencies installed.</p>\n<h2 id=\"5-alternative-human-inspired-mechanism-dreaming\">5. Alternative human-inspired mechanism: dreaming</h2>\n<p>Search of <code>src</code> and <code>extensions</code> (non-test TS) for curiosity/intrinsic reward/information gain/novelty/surprise found relevant dreaming narrative plus unrelated wording. This does not exhaust possible synonyms or third-party extensions.</p>\n<p><code>extensions/memory-core/index.ts:236</code> registers dreaming; <code>extensions/memory-core/src/dreaming.ts:132-155</code> builds a managed cron isolated agentTurn with internal token, lightweight context and no delivery. <code>:914-948</code> service reconciles it at startup. <code>:957-997</code> before_agent_reply checks trigger type and actual managed sentinel/event, then runs promotion. <code>:583-640</code> inspects promotion candidates and components (recall, query diversity, recency, consolidation, conceptual). <code>:671-705</code> can generate a diary narrative from selected memories. This is a maintenance workflow using relevance/promotability, not utility scoring of external explorations.</p>\n<p><code>dreaming-narrative.ts:37-61</code> asks for surprising associations and first-person poetic diary; <code>:245-297</code> calls <code>subagent.complete</code> with narrative prompt and 60-second timeout then appends result. <code>:326-361</code> skips no-input work and optionally detaches via microtask. The inspected path has no call to createGoal, add automation, external research or task queue based on narrative output. It writes a diary. Other dreaming phases exist and were searched, but not exhaustively reviewed, so do not claim entire dreaming subsystem is merely cosmetic: memory consolidation is substantive, diary persona is presentation.</p>\n<h2 id=\"evidence-scope-and-confidence\">Evidence scope and confidence</h2>\n<p>High confidence for caller chains, runtime state transitions and prompt/code distinction above. Reviewed core functions plus supporting caller slices and relevant assertions; no live deployment or provider runs. Negative conclusion: within the inspected native trigger, pacing, goal, Workshop and narrative paths, no novelty/information-gain objective generator choosing new external missions was found. Explicit schedules, durable user objectives, authored policies and evidence-driven maintenance account for observed proactive behavior. Plugins/model behavior may add more, and absence across all possible extensions is not established.</p>\n<h2 id=\"timer-entry-clarified-2026-09-09\">Timer entry clarified (2026-09-09)</h2>\n<p>The scheduled heartbeat path starts before the payload dispatcher. <code>src/cron/service/timer-scheduler.ts:55</code> armTimer reads nextWakeAtMs, computes a bounded delay and calls setCronTimer. At line 115, setCronTimer uses in-process setTimeout; the callback invokes onTimer. onAdmittedTimer (line 170 onward) loads and reserves due jobs before execution. <code>timer-execution.ts:161</code> then branches on payload.kind === \"heartbeat\" or a migrated heartbeat task and calls requestHeartbeatAndWait. The payload describes job behavior; it is not the timer. Wake admission can skip model execution. Event/manual wake entry can bypass the scheduled entry. Rearming belongs to the scheduler and can happen while jobs execute, so a diagram must not imply it only happens after the agent returns.</p>\n<h2 id=\"official-concept-restored-2026-09-09\">Official concept restored (2026-09-09)</h2>\n<p>Same-commit <code>docs/gateway/heartbeat.md:10-23</code> explicitly defines Heartbeat as a system-owned automation whose cadence is owned by the Automations scheduler, without a separate fallback timer. <code>src/cron/heartbeat-monitor.ts:71-120</code> projects config into a system-owned job (<code>schedule.kind=every</code>, <code>payload.kind=heartbeat</code>, <code>sessionTarget=main</code>). The timer-first diagram was incomplete at the feature level; the report now starts with configuration and the monitor job before tracing scheduler/runner code. See <a href=\"official-claims.html\">official claim-to-code review</a> for version-specific documentation and source links.</p>\n<h2 id=\"expanded-mechanism-inventory-2026-09-09\">Expanded mechanism inventory — 2026-09-09</h2>\n<p>See <a href=\"mechanism-inventory.html\">mechanism-inventory.md</a> for additional entry, event, continuation and maintenance paths. Earlier representative flows do not enumerate all mechanisms. New entries are static source traces, not additional live runtime tests.</p>\n<h2 id=\"2026-09-11-follow-up-inferred-commitments\">2026-09-11 follow-up: inferred commitments</h2>\n<p>A separate historical inferred-commitments subsystem existed and was retired. The current main removes the maintenance CLI and migrates recognized old commitment tables away. Earlier pinned runtime analysis remains scoped to its revision; do not infer that the subsystem never existed or that memory/heartbeat restores it. <a href=\"openclaw-inferred-commitments.html\">Historical/current audit</a> · <a href=\"colleague-experience.html\">Colleague experience synthesis</a>.</p>", "reading/research/source-notes/hermes.html": "<h1 id=\"hermes-source-review-2026-09-08\">Hermes source review — 2026-09-08</h1>\n<p>Pinned commit: <code>fef0e16fe19b79ded929209f87c7434270b03825</code> (committer 2026-09-08T06:22:15Z). Source root: <code>/tmp/proactive-agent-research-20260908/source/hermes/hermes-agent-fef0e16fe19b79ded929209f87c7434270b03825</code>.</p>\n<p>Link prefix for every file/line below: <code>https://github.com/NousResearch/hermes-agent/blob/fef0e16fe19b79ded929209f87c7434270b03825/</code>.</p>\n<p>This is a source reading plus three isolated probes of actual extracted functions. It is not an installed-runtime E2E test. <code>python3 -m pytest --version</code> failed: No module named pytest. No dependency installation attempted.</p>\n<h2 id=\"corrections-to-a-documentation-only-account\">Corrections to a documentation-only account</h2>\n<ol>\n<li><strong>Self-paced is lexical backoff, not learned attention.</strong> <code>hermes_cli/loops.py:364-373</code> lowercases text, strips clock strings, dates AND all numeric durations, normalizes whitespace, then SHA-256 hashes it. <code>complete_tick:583-596</code> compares only that digest: identical doubles delay to ceiling; changed resets floor. Actual function probes: <code>latency 2 seconds</code> and <code>latency 40 seconds</code> produce identical digest; <code>No deployment change</code> and <code>Deployment remains unchanged</code> produce different digests. Thus meaningful timing regressions can disappear, while paraphrases can reset the clock. No embeddings, uncertainty or information-gain calculation exists in this path.</li>\n<li><strong>Goal “unchanged workspace” is not content equality.</strong> <code>hermes_cli/goals.py:370-391</code> fingerprints only <code>git rev-parse HEAD</code> plus <code>git status --porcelain</code>. The actual extracted function was run in a temporary git repository: change tracked <code>a.txt</code> to <code>broken</code>, fingerprint; change it to <code>fixed</code>, fingerprint: equal. Both statuses are <code>M a.txt</code>. <code>_check_gates:1272-1326</code> therefore may replay the old failed result and increment attempts even after a content fix. This can prematurely pause a valid repair. Do not present the gate optimization as reliable proof that no work changed.</li>\n<li><strong>Judge failures have earlier stop controls.</strong> <code>goals.py:42-45,1474-1510</code>: consecutive parse failures stop at 3; transport errors at 5. A transient error fails open to continue, but “only the 20-turn cap stops broken judging” is outdated/incomplete.</li>\n<li><strong>20-turn accounting counts ordinary user turns too.</strong> <code>GoalManager.evaluate_after_turn:1437-1462</code> increments <code>turns_used</code> for real messages and synthetic continuation turns; the parameter <code>user_initiated</code> does not alter this increment. Parked waits return before increment. It is not a budget of 20 extra autonomous turns plus unlimited user turns.</li>\n<li><strong>Automatic review is eligible post-turn, not every turn.</strong> Default memory interval 10 user-role turns; skill interval 10 agent iterations. Interval can be disabled; actual tool use resets counters. A successful-looking nonempty final response and no interruption are required for automatic fork spawning, but there is no general semantic quality test for the lesson before the review is asked to judge it.</li>\n</ol>\n<h2 id=\"end-to-end-heartbeat-path\">End-to-end heartbeat path</h2>\n<ul>\n<li><code>hermes_cli/heartbeat.py:18-27</code>: injected prompt carries recurring instruction plus “do not invent work”. This is a behavioral prompt, not a deterministic no-op classifier.</li>\n<li><code>HeartbeatState.is_due:83-90</code>, <code>HeartbeatManager.due_prompt:200-214</code>: active/elapsed test; admission immediately saves new anchor and increments <code>fire_count</code>. Missed intervals coalesce by anchoring at now.</li>\n<li><code>gateway/run_goals.py:116-167</code>: <code>_heartbeat_poll_once</code> iterates watched sessions; <code>_heartbeat_poll_watch</code> re-resolves current session, checks adapter availability, running/active state and queued user messages BEFORE claiming; creates synthetic event and routes <code>adapter.handle_message</code>.</li>\n<li><code>gateway/run_goals.py:169-196</code>: a gateway-wide periodic poller restores watches then polls. Tick opportunity is still time-driven.</li>\n<li><code>gateway/run_heartbeat_acceptance.py:12-35</code>: after normal routing, lineage must be original session or its compression child; check again against current non-suspended session. <code>gateway/run_turn.py:309-310,1955-1962</code> calls ownership checks and sets <code>_heartbeat_execution_started</code> immediately before agent runner admission.</li>\n<li><code>gateway/run_heartbeat_acceptance.py:39-44</code> + <code>HeartbeatManager.abandon_fire:216-231</code>: refund only an exact admission attempt that never entered agent runner, provided persisted state still matches the claim and remains active. Fire count proves execution admission, not success or delivery. This is stronger and more precise than merely “one heartbeat every N minutes.”</li>\n</ul>\n<h2 id=\"goal-judge-continuation-and-evidence-gates\">Goal judge, continuation and evidence gates</h2>\n<ul>\n<li><code>hermes_cli/goals.py:870-930</code> <code>judge_goal</code>: constructs auxiliary-model input from goal (truncated 2,000 chars), most recent final response (4,000 chars), optional completion contract/subgoals and owned background processes/delegation counts. It does not independently read the produced artifact or full tool trace. Contract evidence is prompt-based unless a shell quality gate is configured.</li>\n<li><code>goals.py:393-412</code> <code>run_gate</code>: shell command, timeout, exit code; keeps last output tail. <code>_check_gates:1272-1326</code> gates precede LLM judge; fail means short circuit, replay output into continuation; passes reset attempts. Retry test is <code>attempts &gt; max_retries</code>: default three retries permits initial attempt plus three failures, not exactly three total executions. Replayed cached failures also increment attempts. Fingerprint limitation above applies.</li>\n<li><code>goals.py:1437-1533</code>: active check → waiting check → increment turn → gates → judge → wait/blocked/done handling → persistent failure thresholds → turn cap → render continuation prompt. A blocked verdict pauses, not completes. <code>done</code> can be accepted on last-budget turn; judge is checked before exhaustion pause.</li>\n<li><code>gateway/run_goals.py:309-330</code> <code>_run_post_turn_hooks</code> skips goal judging on empty interrupted/error output but still releases a loop tick. <code>_post_turn_goal_continuation:262-307</code> runs synchronous judge off the event loop and pushes continuation through adapter FIFO; real queued messages retain priority. The agent is repeatedly given user-role continuation prompts; it is not a continuously active neural process.</li>\n<li><code>tests/hermes_cli/test_goal_gates.py:161-242</code>: failing gate short-circuits judge, passing gates permit judge, retry exhaustion and fingerprint change/cache are tested. The cache tests mock <code>workspace_fingerprint</code> as <code>fp-1/fp-2</code>; they do not establish that real content edits necessarily change the fingerprint. Tests inspected, not run.</li>\n</ul>\n<h2 id=\"loop-scheduling-and-stopping\">Loop scheduling and stopping</h2>\n<ul>\n<li><code>hermes_cli/loops.py:429-459</code>: setting requires nonempty prompt, defaults self-paced if no interval; starts due immediately.</li>\n<li><code>loops.py:486-525</code>: <code>fire_tick</code> increments count before execution, saves <code>awaiting_response</code>, provisional next due; no overlap while awaiting. <code>abandon_tick</code> refunds count after injection failure but leaves provisional schedule (does not force instant retry).</li>\n<li><code>loops.py:538-599</code>: completion precedence: model <code>LOOP_COMPLETE</code> marker → optional --until judge → requested times cap (done) → configured max_ticks (paused) → schedule next from end-of-turn. A model self-stop marker can terminate before an --until judge is evaluated; do not describe --until as an unbypassable external evidence gate.</li>\n<li><code>loops.py:468-476</code>: resume sets next due within min(previous delay,5s), but does not reset <code>ticks_fired</code>. Unlike goal resume's reset-budget behavior, resuming a budget-exhausted loop does not replenish its counter; next completed tick can pause again.</li>\n<li><code>loops.py:602-615</code>: active, nonparked goal blocks loop tick to avoid interleaving and consuming goal budget. Waiting/paused/done goals allow loops.</li>\n<li><code>gateway/run_goals.py:368-434</code>: watcher resolves route/adapter, checks running agent and goal blocking, claims tick, sends synthetic event; slash-command loops complete immediately through separate path.</li>\n<li><code>tests/hermes_cli/test_loops.py:341-364,375-521,575-604</code>: tests overlap prevention, markers/caps/judge failures, doubled delays, timestamp normalization and goal blocking. No selected test demonstrates semantic change detection; lexical behavior is intentional.</li>\n</ul>\n<h2 id=\"learning-trigger-fork-allowed-mutation\">Learning trigger → fork → allowed mutation</h2>\n<ul>\n<li><code>agent/agent_init.py:1239-1263,1307-1309</code>: default memory and skill intervals are 10, overridable.</li>\n<li><code>agent/turn_context.py:564-581,854</code>: hydrate memory counter from historical user-role count modulo interval; <code>_tick_memory_nudge</code> requires available memory tool/store, advances and resets when due. Synthetic user turns may count because the mechanism is role/turn based, not human-presence detection.</li>\n<li><code>agent/turn_iteration_prep.py:115-117</code>: increments <code>_iters_since_skill</code> when iteration is prepared and skill_manage exists. Comment says tool-calling iterations, but this increment occurs before provider output is known; it is not a count of successfully executed tools.</li>\n<li><code>agent/tool_executor.py:677-683</code>: invoking memory or skill_manage resets respective counters before actual execution result is known. A failed tool execution can still postpone nudge eligibility.</li>\n<li><code>agent/turn_finalizer.py:591-622</code>: checks skill threshold, resets counter, then nonempty final response + not interrupted + not skip_background_review + due-memory-or-skill causes spawn. Counter reset precedes spawn eligibility; interruptions/disabled review can consume an eligibility opportunity without saving a lesson.</li>\n<li><code>run_agent.py:737-766</code>: automatic review suppressed in delegates, respects enabled switch, structurally clones message snapshot; local managed model may defer until idle. Explicit focused /refine takes a different path. <code>768-813</code> allocates tracked thread/run and bounded preemption requeue behavior.</li>\n<li><code>agent/background_review.py:145-155</code>: 16 maximum review iterations, 600,000 aggregate input-token budget default (configurable; &lt;=0 disables input limit). The 600k is aggregate input, not output budget or total cost guarantee.</li>\n<li><code>background_review.py:845-897</code>: fork disables its own nudges (prevents recursive learning forks), disables provider session finalization, inherits cached prompt/history on same-model path; alternate-model route uses digest.</li>\n<li><code>background_review.py:937-974,993-1043</code>: dispatch whitelist permits memory/skills and read_file/search_files; extras must already exist in parent's schema. Runtime denies terminal/general writing; it is not just a prompt request. Only memory/skill tools mutate learned artifacts in default review.</li>\n<li><code>tools/skill_manager_guards.py:164-238</code>: code denies pinned/external/bundled/hub/user-owned non-curator skills; missing provenance fails closed. Read-before-write guard demands exact target read in this review. Thus learning changes a constrained user-visible library, not arbitrary code or model weights.</li>\n<li><code>tools/memory_tool.py:64-80</code>: approval decision may allow, block or stage. IMPORTANT caveat: import failure of write_approval module returns None (fail-open). Do not call the approval boundary unconditionally fail-closed.</li>\n<li><code>background_review.py:356-362,368-395</code>: prompt prohibits saving unsuccessful methods as reliable workflows, while pushing active learning and updating relevant skills. These are LLM instructions, not experimental validation of lesson truth.</li>\n<li><code>tests/run_agent/test_background_review.py:270-422,464-611</code>: inspected tests cover delegate suppression, enabled switch, snapshot isolation and live-turn preemption. No runtime tests run in this research environment.</li>\n</ul>\n<h2 id=\"search-for-open-ended-exploration-mechanism\">Search for open-ended exploration mechanism</h2>\n<p>Searched actual Python under <code>agent/</code>, <code>hermes_cli/</code>, <code>gateway/</code>, <code>tools/</code> for curiosity, intrinsic_reward, information_gain, spontaneous, self_generated and exploration. No scheduler/policy implementing endogenous root-goal selection, uncertainty reduction, curiosity rewards or research-question generation found in these examined execution paths.</p>\n<p>An instructive positive hit is <code>agent/prompt_builder.py:129-138</code>: maintainer comment says models UNDER-explore by default and forbids restoring an exploration-thrift line. <code>hermes_cli/default_soul.py:3-17</code> deliberately aligns default persona. That is <strong>permission for sufficient task exploration through prompt design</strong>, not an implemented spontaneous curiosity policy. It should temper a claim that Hermes actively discourages exploration; it discourages unnecessary heartbeat busywork while allowing deeper user-task investigation.</p>\n<p>Actual identity construction: <code>agent/system_prompt.py:491-493</code> chooses profile SOUL when <code>load_soul_identity</code> or context files enabled, otherwise fallback <code>DEFAULT_AGENT_IDENTITY</code>. <code>agent/prompt_builder.py:1440-1473</code> loads profile-pinned SOUL, strips legacy bot protocol, scans and truncates; missing/failed read yields fallback. Therefore custom SOUL may replace this default, and the UNDER-explore sentence itself is a maintainer code comment, not text shown to the model. The implemented effect is removal of the prior thrift sentence from both fallback and seeded SOUL.</p>\n<p>Conclusion: Hermes implements externally seeded persistent goals, adaptive lexical pacing, foreground-prioritized wakeups and bounded experience consolidation. It can choose next actions and reflect without another user message. The source examined does not show a default endogenous objective generator or intrinsic-motivation exploration controller. This is scoped negative evidence, not a proof of absence across all plugins/extensions.</p>\n<h2 id=\"probe-results-actual-functions-extracted-by-ast-no-algorithm-reimplementation\">Probe results (actual functions extracted by AST; no algorithm reimplementation)</h2>\n<pre><code>DIGEST_CHANGED_DURATION_COLLIDES True\nDIGEST_PARAPHRASE_NOT_EQUAL True\nFINGERPRINT_CONTENT_CHANGE_WITH_SAME_STATUS_COLLIDES True\n/opt/homebrew/opt/python@3.14/bin/python3.14: No module named pytest\n</code></pre>\n<p>The fingerprint probe initialized a disposable temp git repo, committed a tracked file, changed its contents twice while preserving dirty-status shape, and called the pinned function both times. No source edits were made. Treat findings as reproducible implementation properties; deployed impact depends on configured gates/loops and actual workload.</p>\n<h2 id=\"expanded-mechanism-inventory-2026-09-09\">Expanded mechanism inventory — 2026-09-09</h2>\n<p>See <a href=\"mechanism-inventory.html\">mechanism-inventory.md</a> for additional entry, event, continuation and maintenance paths. Earlier representative flows do not enumerate all mechanisms. New entries are static source traces, not additional live runtime tests.</p>\n<h2 id=\"cron-monitor-mode-observation-gate-2026-09-09\">Cron monitor mode — observation gate (2026-09-09)</h2>\n<p>See observation-to-action.json and observation-probes.json. check_monitor compares exact source-output hashes before agent setup; first observation also runs. Baseline persistence precedes downstream execution and logs failures, so it is not a delivery acknowledgement or a reliable retry ledger. A script wakeAgent=false gate is distinct from monitor hash comparison.</p>", "reading/research/source-notes/openbot.html": "<h1 id=\"openbot-pinned-source-review\">OpenBot pinned source review</h1>\n<p>Reviewed 2026-09-08. Repository CopilotKit/OpenBot, commit <code>7b94a0b802732e6491634160cf9ed3fcfb813424</code> (2026-09-06T21:06:23Z). Local root <code>/tmp/proactive-agent-research-20260908/source/openbot/OpenBot-7b94a0b802732e6491634160cf9ed3fcfb813424</code>. This is source/test inspection, NOT execution of the tests and NOT live deployment validation. No dependencies installed. OpenBot identity remains a provisional interpretation of the user's ambiguous “open bot”.</p>\n<p>All relative file+line references below resolve at https://github.com/CopilotKit/OpenBot/blob/7b94a0b802732e6491634160cf9ed3fcfb813424/ (append #L<number>).</p>\n<h2 id=\"main-conclusion\">Main conclusion</h2>\n<p>The source implements user-delegated scheduled work plus dynamic task handoff, with durable identity, permissions, history and execution controls. It does not furnish evidence of an endogenous curiosity/novelty scoring process, a self-generated curriculum, or a standing pool of candidate goals selected during idle time. The existing agent can make tool/next-step decisions inside a prompted run, and a routine instruction may authorize exploration or schedule mutation; those are extension points, not a built-in intrinsic-motivation subsystem.</p>\n<h2 id=\"complete-trigger-to-execution-chain\">Complete trigger-to-execution chain</h2>\n<ol>\n<li>\n<p><strong>Conversation becomes a tool call.</strong> <code>server/src/plugins/builtin-routines.ts:74-124</code> declares <code>create_routine</code> with <code>instruction</code>, five-field <code>cron</code>, optional timezone and channel. The model translates the user's sentence; this is prompt-mediated interpretation. Its tool description explicitly separates the work of ONE firing from the schedule. Runtime <code>callTool</code> at <code>:356-398</code> takes owner from <code>connection.actorId</code> and bot from <code>connection.botId</code>, ignores spoofed owner/bot arguments, then calls the store. Actual tool availability and invocation are permission mediated: <code>server/src/plugins/tools.ts:157-183</code> loads granted tools and calls <code>store.callTool</code>; <code>server/src/plugins/store.ts:2785-2812</code> rechecks <code>decide(\"mcp\", input.ref, input.botId)</code> and refuses before invocation. No model permission assertion substitutes for these checks.</p>\n</li>\n<li>\n<p><strong>Persist intent and next occurrence.</strong> <code>server/src/routines/store.ts:512-550</code> validates instruction, resolves a channel in which the owner belongs, defaults timezone UTC, computes <code>nextRunAt</code>, then inserts under <code>withEnabledCapLock</code> (<code>:424-433</code>). This lock uses Postgres advisory transaction locking around the per-owner 20-enabled-routine count so racing creates cannot bypass the cap. The schema <code>server/src/db/schema/coworker.ts:93-125</code> stores ownerUserId, agentId, channelId, instruction, cron, timezone, enabled, nextRunAt and lastRunAt. It is a schedule ledger, not a goal generator. <code>server/src/routines/schedule.ts:100-173</code> validates sub-floor intervals and obtains cron-parser next occurrences; minimum interval is 15 minutes. Cron/timezone/enabling updates recompute from current time (<code>store.ts:450-505</code>).</p>\n</li>\n<li>\n<p><strong>The waking process is deterministic polling.</strong> <code>worker/src/index.ts:88-105</code> uses <code>TICK_MS=30_000</code>; <code>runOneTick</code> at <code>:110-129</code> runs <code>offerDueRoutines</code> then <code>dispatchClaimedRoutines</code>; the main loop at <code>:163</code> sleeps after a tick. It invokes no idle-reflection model prompt. Kubernetes uses the same sweep via <code>server/scripts/fire-routines.ts</code> from an external CronJob.</p>\n</li>\n<li>\n<p><strong>Offer due work and move the schedule.</strong> <code>server/src/routines/store.ts:663-688</code> selects enabled rows with <code>nextRunAt &lt;= now()</code> using database clock. <code>server/src/routines/sweep.ts:135-196</code> offers key <code>&lt;routine-id&gt;:&lt;UTC-due-minute&gt;</code> to the shared queue before advancing the stamp. <code>store.ts:690-735</code> compare-and-sets the old stamp. Stale occurrences outside the 10-minute grace are skipped and the next occurrence is calculated from now. This is a temporal relevance heuristic, not learned value-of-information selection. The next occurrence is scheduled BEFORE the current model run finishes; completion does not generate a successor goal.</p>\n</li>\n<li>\n<p><strong>Claim and dispatch.</strong> <code>server/src/work/queue.ts:268-330</code> uses a transaction, <code>FOR UPDATE SKIP LOCKED</code>, database-clock lease eligibility and attempts &lt; cap, then records lease owner and increments attempts. <code>sweep.ts:260-443</code> claims, renews, rereads whether routine is enabled/present, checks age, creates a run row, calls dispatch, and finishes the queue row after HTTP acceptance. <code>worker/src/index.ts:66-84</code> posts the run id to the internal API with a bearer secret and bounded HTTP wait. <code>server/src/app.ts:812-878</code> validates the secret/body, invokes detached <code>routineRunner.run(...)</code>, and returns 202. Queue delivery acceptance is NOT model success.</p>\n</li>\n<li>\n<p><strong>Resolve current owner and build the actual agent.</strong> <code>server/src/routines/runner.ts:72-135</code> reloads run context and owner-accessible channel; missing channel yields skipped without model spend. <code>server/src/index.ts:661-703</code> calls <code>actorFor(ownerUserId)</code> freshly, loads actor-visible agents, actor tools, signed run identity, current standing instructions, and selects the routine's agent. Deleted/invisible agent throws. <code>server/src/copilot.ts:271-300</code> composes built-in prompt from agent systemPrompt, user standing instructions, provenance and current capabilities. Thus persisted identity does not freeze obsolete rights.</p>\n</li>\n<li>\n<p><strong>Frame a firing, seed history, run.</strong> <code>server/src/routines/run-turn.ts:246-253</code> wraps instruction: it is firing now; perform the work this turn; do not create/list/change routine unless instruction asks. This is a PROMPT restriction, not a tool removed from the agent. <code>:281-399</code> creates/gets thread, retrieves/sanitizes history, adds the framed user message, builds agent, sets its messages/thread, passes no frontend tools, and acquires a thread lock. <code>:458-482</code> actually calls <code>runner.run({threadId, agent, input, persistedInputMessages})</code>; RUN_ERROR emitted as a normal observable event is explicitly converted to failure.</p>\n</li>\n<li>\n<p><strong>What heartbeat really does.</strong> <code>run-turn.ts:86</code> sets 15-second heartbeat; <code>:437-452</code> only calls <code>ɵrenewThreadLock</code>. It does NOT read workspace, choose a goal, reflect, or invoke an LLM. Failure clears heartbeat and calls <code>agent.abortRun()</code> plus <code>runner.stop()</code> (<code>:425-435</code>). The 5-minute deadline and 5-second abort grace are at <code>:76-79</code> and <code>:485-499</code>; finally releases lock and timers (<code>:502-520</code>). Other heartbeat variables in <code>agents/handoff-runner.ts:263</code> and <code>handoff-delivery.ts:325</code> similarly renew ownership/locks.</p>\n</li>\n<li>\n<p><strong>Completion and unattended limits.</strong> <code>run-turn.ts:522-570</code> rejects heartbeat loss, timeout, pending human interrupts, and an empty reply. A routine that asks for human input is marked failed, not retained here as a pause/resume workflow. <code>runner.ts:137-178</code> records failed status, sends first failure notice, disables after 10 consecutive failures, or posts reply and records succeeded. <code>store.ts:813-842</code> finishes only an unfinished run. Success is execution completion with reply, not a semantic proof that business work was correctly accomplished.</p>\n</li>\n<li>\n<p><strong>Pause does not establish in-flight cancellation.</strong> <code>store.ts:632-636</code> setEnabled delegates to update; re-enable recomputes next time. Sweep rereads enabled before dispatch and drops queued work when disabled. <code>runContext</code> only selects routine id, owner, bot, channel and instruction (<code>store.ts:758-780</code>); it has no enabled field, and runner has no enabled/cancellation check. Source therefore supports blocking future/pending firings, not a claim that the routine toggle aborts an already accepted turn. The common thread runner has independent abort/deadline controls.</p>\n</li>\n</ol>\n<h2 id=\"dynamic-handoff-initiative-inside-a-task\">Dynamic handoff: initiative inside a task</h2>\n<p><code>server/src/agents/handoff-tool.ts:23-44</code> uses structured bot/task/constraints/expecting; <code>:74-94</code> offers no tool without grants or at depth/fanout disable. The model decides when delegation helps, but authority and budgets are code. <code>handoff.ts:145-170</code> checks signed depth; <code>:191-271</code> resolves current actor-visible target, rejects missing/self target, and rereads grants; <code>:295-330</code> hashes actor/run/target/task/constraints/expecting for idempotence and atomically offers under per-run cap. Repeated content is not new work. At cap, it instructs model to answer with what it has or ask the person (<code>:350-356</code>).</p>\n<p>Delivery is event-driven: <code>server/src/index.ts:972-1009</code> serializes per-replica sweep, uses PostgreSQL work-offered notification plus 2-second polling backstop. It reacts to queued <code>bot.message</code>, not environmental novelty. <code>:895-923</code> builds target as current actor, executes target on a scratch thread and relays answer to original conversation (implementation comments warn thread ownership means original asking Bot's voice, so avoid simplistic claim every target posts directly as itself in original thread). <code>handoff-runner.ts:263-274</code> renews queue lease. These workers do not spontaneously populate the queue.</p>\n<p>The example remote LangGraph loop (<code>agent-langgraph/src/index.ts:331-385</code>) runs answer -&gt; deployment tools -&gt; answer until no tool call, or ends when tool requires a surface/human. It is a model-controlled action loop within an input run, not a repeated unsupervised mind. Comment mentions recursionLimit, but invocation at <code>:404-410</code> supplies only <code>{version:\"v2\"}</code> and no explicit numeric recursionLimit: do not invent an OpenBot-specific max-step value from that comment. Built-in actors instead use CopilotKit agent runtime; remote AG-UI endpoints can implement other loops.</p>\n<h2 id=\"tests-inspected-not-run\">Tests inspected (not run)</h2>\n<ul>\n<li><code>server/tests/builtin-routines.test.ts:158-180</code>: verifies connection actor/bot + instruction/cron/timezone/channel reach create. <code>:271-308</code> supplies someone-else/another-bot args and verifies they are ignored across create/list/update/delete.</li>\n<li><code>server/tests/routine-schedule.test.ts:28-95</code>: floor covers every minute, five minute, disjoint yearly/overnight adjacent pairs, accepts valid interval; <code>:10-26</code> checks local 09:30 across DST.</li>\n<li><code>server/tests/routine-sweep.integration.test.ts:276-302</code>: two actual DB sweeps in Promise.all leave one work row and exactly one stamp advance. <code>:427-451</code>: month-old task creates no firing, recent one fires, stale nextRunAt catches up in one pass. <code>:684-704</code>: disable after offer produces no dispatch/no run row and finishes work item. These are integration test definitions, not evidence they passed today.</li>\n<li><code>server/tests/routine-run-turn.test.ts:238-316</code>: mocks assert thread creation before lock before run, seeded history plus firing message. <code>:337-380</code> documents previous live regression: schedule-shaped instruction caused list_routines + already-configured answer without actual work; asserts framing now distinguishes execution from setup. This illustrates why scheduler + prompt is not semantic task success.</li>\n<li><code>routine-run-turn.test.ts:621-657</code>: forced timeout/renew failure aborts, releases lock, stops renewing. <code>:821-862</code>: empty output and pending human interrupt throw; tests specifically do not treat either as a completed background task.</li>\n<li><code>server/tests/routine-runner.test.ts:214-232</code>: tenth failure finishes failed, disables, sends final message; <code>:235-245</code> no accessible channel means no turn, skipped ledger.</li>\n<li><code>server/tests/agent-handoff.test.ts:176-187</code>: signed depth at cap yields refusal and no queued row; <code>:260-286</code> no grant/self prohibited. These reinforce bounded task delegation, not endogenous goals.</li>\n</ul>\n<h2 id=\"limits-and-substantive-absence-assessment\">Limits and substantive absence assessment</h2>\n<p>Inspected control flow above, runtime prompt composition and shipped tenant prompts (<code>examples/fintech/agents.yaml</code>), all database schema inventories (<code>server/src/db/schema/*</code>), routine/task queue producers and consumers, plus whole repository .ts/.md/.yaml keyword pass (excluding tests) for heartbeat/proactive/curiosity/intrinsic/curriculum/webhook/exploration. Only lease/lock heartbeat matches appeared. Schema offers roles/instructions, channels/history mapping, routine schedule/run status and generic queued payload, with no explicit opportunity store, curiosity score, uncertainty frontier or exploration curriculum. Core wake paths inspected consume schedules, user turns, or existing handoff work. No inspected prompt says idle agents should invent and pursue independent goals. This bounded source audit supports “no built-in autonomous exploration mechanism evidenced in reviewed core”; it cannot rule out custom plugins, arbitrary remote AG-UI agents, tasks encoded as free text, or model behavior inside authorized tools.</p>\n<p>Reliability qualification: <code>sweep.ts:426-445</code> warns <code>routine-fire-redelivery-possible</code> when lease expires after dispatch; HTTP timeout can also mean accepted-but-unacknowledged (<code>:405-416</code>). Therefore source has idempotent offering/concurrent claim protections, not unconditional exactly-once external side effects. No production execution or test suite was run in this review.</p>\n<h2 id=\"expanded-mechanism-inventory-2026-09-09\">Expanded mechanism inventory — 2026-09-09</h2>\n<p>See <a href=\"mechanism-inventory.html\">mechanism-inventory.md</a> for additional entry, event, continuation and maintenance paths. Earlier representative flows do not enumerate all mechanisms. New entries are static source traces, not additional live runtime tests.</p></article><footer class=\"article-footer\"><a href=\"../../../library.html\">← 回研究目錄</a><a href=\"grok-reconstructed.html\">下一篇：Grok Bot 0.18: reconstructed implementation review →</a></footer>", "reading/research/source-notes/grok-reconstructed.html": "<h1 id=\"grok-bot-018-reconstructed-implementation-review\">Grok Bot 0.18: reconstructed implementation review</h1>\n<p>Inspected on 2026-09-08 at commit <code>a9f633e09d49a85829b8236331b9e21f7e612634</code> in <code>b-nnett/grok-bot-0.18-reconstructed</code>.</p>\n<h2 id=\"provenance-and-scope\">Provenance and scope</h2>\n<p>This is an unofficial reconstruction with extensions, not original vendor source. <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/PROVENANCE.md#L1\">PROVENANCE.md</a> identifies a macOS Grok Bot 0.18 application, an Anysphere bundle identity, and a Cursor-hosted installer. These are repository provenance claims. The original binary, signature, and behavioral equivalence were not independently validated. They must not be generalized to an xAI cloud controller or another product sharing a Grok name.</p>\n<p>The repository also adds inference routing and a local Docker option. A readable implementation and its tests can establish reconstruction behavior; they cannot alone establish which behavior existed in the shipped application. The reviewed source directory was compared with the pinned Git revision and has no diff. Large installer preservation assets were not needed for this review.</p>\n<h2 id=\"automation-delivery-defined-work-arrives-from-scheduling-infrastructure\">Automation delivery: defined work arrives from scheduling infrastructure</h2>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/automations/extension.ts#L73\">extension.ts:73</a> wires cloud-definition synchronization, a fire consumer, notification drains, and local event listeners. Reconciliation calls fireConsumer.tick; local Slack/GitHub event handling passes through SandTriggerHub and its scheduling-authority checks. This does not establish a locally implemented cloud cron service.</p>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/automations/sand-automation-fire-consumer.ts#L72\">SandAutomationFireConsumer.tick:72</a> prevents overlapping polls, checks readiness/backoff, polls backend fire events, tracks running/completed/reported states, and acknowledges reported work on later polls. deliver resolves current target definitions, reuses existing completed outcomes, rejects disabled or changed definitions, and validates event-trigger matching before invoking execution. Delivery errors can leave work available for later retry. This is not unconditional exactly-once side-effect delivery.</p>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/automation-run-path.ts#L109\">AutomationRunPath.fireAutomation:109</a> checks execution availability, prevents duplicate in-flight non-event firings, resolves the background session, and applies the background spend guard where applicable. It records run state and enqueues a background-lane turn. The current automation builds the prompt; runner.run receives hidden and isSilenceAllowed. Group automations branch into a different execution path. Finally it records run status, updates automation state and telemetry, and releases the in-flight marker.</p>\n<p>The important boundary is defined automation versus new objective selection. These functions consume configured work and event context. They do not independently generate a new root objective.</p>\n<h2 id=\"event-and-completion-wakes-continue-owned-work-without-a-fresh-user-message\">Event and completion wakes: continue owned work without a fresh user message</h2>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/background-wakes.ts#L359\">BackgroundWakes:359</a> appends timeline events, queues eligible wakes when the agent is not running, batches pending material, resolves background sessions, and invokes runBackgroundWake. Group sessions are excluded from this event path.</p>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/completion-revivals.ts#L99\">CompletionRevivals:99</a> has distinct subagent and shell completion queues. The subagent path deduplicates pending completions by subagent identity. runSubagentRevival constructs a result-bearing prompt and enqueues an exclusive background run with silence allowed.</p>\n<p>Quiet-origin prompts distinguish a standing-order result from work the user is waiting for. They ask the model to report useful change or actionable results and otherwise remain silent. This is model guidance, not a deterministic information-value classifier.</p>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/run-scheduler.ts#L199\">RunScheduler.pump:199</a> prioritizes queued user tasks over agent and background tasks. Its watchdog can interrupt a wedged run and later release the logical slot. escapeWedgedRun retains the unsettled promise as a zombie while scheduling proceeds. Queue progress therefore does not prove physical cancellation or unlimited freedom from overlapping work.</p>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/pending-wake-rearm.ts#L99\">PendingWakeRearm:99</a> restores eligible persisted watches and prunes stale markers. A lost in-process subagent produces an interrupted/unknown-state completion notice; it is not represented as successfully resumed execution. These persistence semantics belong to completion watches, not arbitrary autonomous missions.</p>\n<h2 id=\"goal-continuation-handler-exists-automatic-producer-not-established\">Goal continuation: handler exists, automatic producer not established</h2>\n<p><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent/index.ts#L72\">agent/index.ts:72</a> registers GoalContinuationActionHandler. <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent/actions/goal-continuation-action-handler.ts#L84\">adaptAction:84</a> validates an active goal's UUID and conversation/session ownership. After three prior continuations without counted tools, it pauses the goal and deactivates the clock. Otherwise it increments continuation counters, updates active-time accounting, and constructs simulated user input containing the objective.</p>\n<p>handle delegates to the ordinary user-message handler under tool-counting middleware. Counted tool activity clears the idle streak. handleModelStep can clear it based on generated tool descriptors, before actual execution; this is an especially weak proxy for useful progress. The three-idle-turn guard is not a total continuation budget or proof that work has stalled semantically.</p>\n<p>Searching the inspected source for goalContinuationAction and its handler found registration, dispatch support, type definitions, and handler logic. It did not establish a producer that automatically submits the next action after every turn. The diagram therefore starts at receipt of that action and has no asserted automatic loop-back edge. This is deliberately narrower than calling the reconstruction a proven repeat-until-done goal scheduler.</p>\n<h2 id=\"exploration-interpretation\">Exploration interpretation</h2>\n<p>The inspected runner, transcript, and agent-core paths support configured work, event-triggered follow-up, and continuation actions. No intrinsic-reward, curiosity-score, or information-gain task generator was found in those paths. Task-level proactive prompt guidance exists; it is not equivalent to a root-goal generator. Custom extensions, model behavior, and remote services remain outside this negative conclusion.</p>\n<p>Interpretation from code: the reconstruction emphasizes resuming useful work from explicit triggers while managing ownership, silence, and interruption. This is an inference, not an authenticated author statement.</p>\n<h2 id=\"executed-checks\">Executed checks</h2>\n<p>Seven checks directly imported unchanged TypeScript pure functions under Node's type stripping:</p>\n<ol>\n<li>Identical conversation/id wake is deduplicated.</li>\n<li>Same-conversation group wake replaces its predecessor.</li>\n<li>Same group key in a different conversation does not replace it.</li>\n<li>Mismatched goal session ownership is rejected.</li>\n<li>Matching legacy conversation ownership is accepted.</li>\n<li>Active elapsed time adds time since the clock anchor.</li>\n<li>Deactivation accrues time and clears the anchor.</li>\n</ol>\n<p>Result: 7/7 passed. Files: <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent-exec/wakeup/index.ts#L1\">wake merge</a>, <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent-core/goal-continuation.ts#L1\">goal-state helpers</a>.</p>\n<p>No dependency installation, full test suite, model call, app launch, cloud scheduler execution, goal-handler integration test, or original-binary comparison was performed.</p>\n<h2 id=\"expanded-mechanism-inventory-2026-09-09\">Expanded mechanism inventory — 2026-09-09</h2>\n<p>See <a href=\"mechanism-inventory.html\">mechanism-inventory.md</a> for additional entry, event, continuation and maintenance paths. Earlier representative flows do not enumerate all mechanisms. New entries are static source traces, not additional live runtime tests.</p>\n<h2 id=\"official-product-context-checked-2026-09-11\">Official product context, checked 2026-09-11</h2>\n<p>The <a href=\"https://x.ai/news/introducing-grok-bot\">official Grok Bot page</a> describes persistent follow-up, learned routines and Bot coordination. This supports product intent independently of the reconstruction, but does not authenticate this repository or establish implementation parity. <a href=\"colleague-experience.html\">Colleague experience synthesis</a> maps official claims, reconstruction mechanisms and our proposed implementation separately.</p><details class=\"article-references\"><summary>本文參考來源 · 13</summary><p>以下保留本文實際引用的來源；引用不等於實測，適用範圍以正文說明為準。</p><ol><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/PROVENANCE.md#L1\">PROVENANCE.md</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/automations/extension.ts#L73\">extension.ts:73</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/automations/sand-automation-fire-consumer.ts#L72\">SandAutomationFireConsumer.tick:72</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/automation-run-path.ts#L109\">AutomationRunPath.fireAutomation:109</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/background-wakes.ts#L359\">BackgroundWakes:359</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/completion-revivals.ts#L99\">CompletionRevivals:99</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/run-scheduler.ts#L199\">RunScheduler.pump:199</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/pending-wake-rearm.ts#L99\">PendingWakeRearm:99</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent/index.ts#L72\">agent/index.ts:72</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent/actions/goal-continuation-action-handler.ts#L84\">adaptAction:84</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent-exec/wakeup/index.ts#L1\">wake merge</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/packages/agent-core/goal-continuation.ts#L1\">goal-state helpers</a></li><li><a href=\"https://x.ai/news/introducing-grok-bot\">official Grok Bot page</a></li></ol></details>", "reading/research/source-notes/voyager.html": "<h1 id=\"voyager-implementation-review\">Voyager implementation review</h1>\n<p>Pinned revision: <code>55e45a880755d0c8c66ca7fb5fe7962ac8974f89</code>.</p>\n<p><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L295\">learn()</a> calls the curriculum to propose a task, runs a rollout, stores a skill on success, and updates exploration progress before selecting another task. This is direct evidence of an outer task-selection loop under an assigned exploration objective.</p>\n<p><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/curriculum.py#L240\">propose_next_task()</a> contains a fixed initial wood-log task and an inventory-dependent branch, with model generation on the normal path. Task selection is therefore a combination of program branches and model judgment.</p>\n<p><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L203\">step()</a> invokes the action model, executes environment actions, and uses observations and critic feedback to revise execution. <a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/critic.py#L91\">The critic</a> parses a model success judgment. This is not a deterministic proof of success.</p>\n<p>Novelty and difficulty instructions guide curriculum generation. The inspected path does not implement a scalar information-gain optimizer or demonstrate human-like intrinsic motivation. Interpretation: Voyager delegates selecting the next bounded task, beyond merely continuing the same task.</p>\n<p>Six isolated checks exercised original class bodies with mocked collaborators: initial task, inventory branch, valid and invalid task parsing, and success/failure skill-persistence branches. No model calls, Minecraft session, or full upstream test suite were executed.</p>\n<h2 id=\"prioritization-clarification-2026-09-10\">Prioritization clarification · 2026-09-10</h2>\n<p>The normal path directly requests one Task from the LLM. Novelty, feasible difficulty and necessary resource gathering are prompt criteria without explicit weights. Startup/inventory branches take precedence. Updating history is contextual adaptation; the inspected path does not train a priority predictor. See <a href=\"prioritization.html\">prioritization audit and research comparison</a>.</p><details class=\"article-references\"><summary>本文參考來源 · 4</summary><p>以下保留本文實際引用的來源；引用不等於實測，適用範圍以正文說明為準。</p><ol><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L295\">learn()</a></li><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/curriculum.py#L240\">propose_next_task()</a></li><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L203\">step()</a></li><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/critic.py#L91\">The critic</a></li></ol></details>", "reading/research/source-notes/proactive-trigger-mechanisms.html": "<h1 id=\"主動的觸發與進階機制timeevent-之外到底還有什麼\">主動的觸發與進階機制：time、event 之外到底還有什麼？</h1>\n<p>查核：2026-09-11。回到 <a href=\"../README.html\">研究入口</a> · <a href=\"../../../show-me-proactive-lifecycle.html#proactive-overview\">高層互動圖</a>。本次重新閱讀下列固定版本的代表路徑及目前官方文件，不宣稱完整窮舉所有 plugin。</p>\n<h2 id=\"給決策者的答案\">給決策者的答案</h2>\n<p><strong>喚醒層用 time／event 足夠描述我們的設計。進階功能多半改進觀察、判斷、記憶、接續與可靠性，沒有另外創造一種無來源的「念頭」。</strong> 有些工作持續在執行，不是每一步都重新喚醒。</p>\n<p>這個分類是工程抽象，不是關於人類意識的結論。「event」若定義得足夠廣，本來就能包含 timer；我們分開列，是因為定時 polling 與被動接收通知的延遲、成本和恢復方式不同。</p>\n<h2 id=\"三個不同層次不混在同一張觸發清單\">三個不同層次，不混在同一張觸發清單</h2>\n<div class=\"table-scroll\" role=\"region\" aria-label=\"研究比較表\" tabindex=\"0\"><table>\n<thead>\n<tr>\n<th>層次</th>\n<th>要回答的問題</th>\n<th>實例</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>開始／恢復</td>\n<td>這輪為什麼有執行機會？</td>\n<td>timer、新訊息、工作完成、thread idle</td>\n</tr>\n<tr>\n<td>執行控制</td>\n<td>已在工作的程式怎麼走下一步？</td>\n<td>while loop、工具結果、workflow dependency、continue／wait／stop</td>\n</tr>\n<tr>\n<td>任務政策</td>\n<td>現在做什麼、下次要看什麼？</td>\n<td>prompt 推論、候選排序、curriculum、記憶反思、learning progress</td>\n</tr>\n</tbody>\n</table></div>\n<p><code>while</code> 迴圈直接下一次迭代，不需要存在一個 event bus。可以在高層把「前一步完成」視作內部事件，但不能把這個抽象寫成實際採用了事件中介軟體。</p>\n<h2 id=\"七個框架剝掉名稱後的機制\">七個框架：剝掉名稱後的機制</h2>\n<div class=\"table-scroll\" role=\"region\" aria-label=\"研究比較表\" tabindex=\"0\"><table>\n<thead>\n<tr>\n<th>框架／查核路徑</th>\n<th>執行機會怎麼來</th>\n<th>多做的部分</th>\n<th>沒有因此證明什麼</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OpenClaw Heartbeat／Automations</td>\n<td>排程到期；符合條件的事件請求 wake</td>\n<td>背景容量、scope、模型判斷與選擇性投遞；eligible pacing 接受 next-check delay</td>\n<td>不等於內建通用承諾管理器；inferred commitments 已移除</td>\n</tr>\n<tr>\n<td>Hermes cron monitor／loop／goal</td>\n<td>定時；loop／goal 接續既有執行</td>\n<td>來源 hash gate、重複回應退避、goal 判斷與限制</td>\n<td>hash 不是語意價值分數；接續不等於產生根目標</td>\n</tr>\n<tr>\n<td>OpenBot routines</td>\n<td>due sweep 找到到期工作，放入 queue 給 worker</td>\n<td>stale window、排程 occurrence 身分、claim／去重</td>\n<td>queue priority 不代表模型知道哪件工作最有價值</td>\n</tr>\n<tr>\n<td>Grok Bot 0.18 非官方重建</td>\n<td>automation fire、背景結果、Bot 訊息等事件</td>\n<td>從持續需求建立 routine；completion 回到等待對話；安靜執行與 SendMessage 分離</td>\n<td>不能用重建碼證明官方 backend；goal handler 存在不等於已找到自動 producer</td>\n</tr>\n<tr>\n<td>Codex /goal 固定版本</td>\n<td><code>on_thread_idle</code> → <code>continue_if_idle</code> → <code>start_turn_if_idle</code></td>\n<td>active goal、deferral、thread 存活與狀態鎖檢查</td>\n<td>不是另一個外界觀察來源，也不表示另有獨立 completion judge</td>\n</tr>\n<tr>\n<td>Claude Code 官方文件</td>\n<td>/loop 等時間；/goal 在回合後評估，等待時另有 idle check-in</td>\n<td>動態間隔、Monitor、獨立小模型評估完成條件</td>\n<td>Monitor 背景腳本仍可能 polling；評估器不自行讀檔或執行命令</td>\n</tr>\n<tr>\n<td>Voyager</td>\n<td><code>learn()</code> 持續 while，前一 rollout 結束後選下一任務</td>\n<td>環境／成敗 context → curriculum 選題；成功程式保存為 skill</td>\n<td>不需要 cron；也不是不需執行中的程式就自己生成任務</td>\n</tr>\n</tbody>\n</table></div>\n<p>各列來源見下方固定路徑。Claude Code 為 2026-09-11 官方文件契約；其他原始碼以列出的 SHA 為準，未宣稱與所有最新發行版一致。</p>\n<h2 id=\"具體有哪些值得借鏡的進階設計\">具體有哪些值得借鏡的進階設計？</h2>\n<h3 id=\"1-先便宜地看再決定要不要叫模型\">1. 先便宜地看，再決定要不要叫模型</h3>\n<p>Hermes <code>cron/monitor.py</code> 先執行已設定的 script 或 URL，hash 與上次一樣就 early return；改變才提供輸出／diff 給後續 agent。這降低空轉模型成本，但 timestamp、排序變動可能造成假變化；baseline 在模型成功前保存，也不能當作送達成功的證據。</p>\n<p>來源：<a href=\"https://github.com/NousResearch/hermes-agent/blob/fef0e16fe19b79ded929209f87c7434270b03825/cron/monitor.py#L125\">monitor.py L125</a> · <a href=\"observation-hermes.html\">完整 caller 與限制</a>。</p>\n<h3 id=\"2-模型選下次何時再看\">2. 模型選下次何時再看</h3>\n<p>OpenClaw 的 eligible pacing path 把 <code>nextCheck.delayMs</code> 經 pacing 限制轉成下一次執行時間；不是所有工作都無條件接受。Claude Code 的動態 <code>/loop</code> 也依狀況選間隔。這是「決策生成排程」，之後仍由 timer 執行。Hermes self-paced loop 則以回應 digest 是否重複調整節奏，不等於對環境計算 information gain。</p>\n<p>來源：<a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/src/cron/service/timer-outcomes.ts#L481\">OpenClaw timer-outcomes.ts L481</a> · <a href=\"https://github.com/NousResearch/hermes-agent/blob/fef0e16fe19b79ded929209f87c7434270b03825/hermes_cli/loops.py#L580\">Hermes loops.py L580</a> · <a href=\"https://code.claude.com/docs/en/scheduled-tasks\">Claude Code scheduled tasks</a>。</p>\n<h3 id=\"3-完成不是一句話有接續與評估\">3. 完成不是一句話：有接續與評估</h3>\n<p>Codex 固定版本在 idle 時檢查既有 active goal，使用狀態鎖防止目標在讀取／啟動之間改變，再嘗試開下一輪。Grok 重建會把背景完成結果排回對話。Claude Code <code>/goal</code> 則明確描述每輪後另有小模型判斷條件，未達成再開回合；該評估器依賴對話已呈現的證據，不獨立執行驗證。</p>\n<p>來源：<a href=\"https://github.com/openai/codex/blob/634ebc1865c6ac840ed3ba118f040d527bf4b55d/codex-rs/ext/goal/src/runtime.rs#L399\">Codex runtime.rs L399</a> · <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/completion-revivals.ts#L99\">Grok completion-revivals.ts L99</a> · <a href=\"https://code.claude.com/docs/en/goal\">Claude Code goal</a>。</p>\n<h3 id=\"4-整理記憶產生未來工作\">4. 整理記憶、產生未來工作</h3>\n<p>OpenClaw dreaming 在查核版本建立 managed cron job，並限定相關 heartbeat／cron trigger 進入整理；記憶排序／整理是回合裡的工作。Grok 重建的 routine prompt 鼓勵把明確持續需求保存為未來指令與 trigger。兩者可讓未來更有脈絡，但不是「模型在休眠時自行思考」。</p>\n<p>來源：<a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/extensions/memory-core/src/dreaming.ts#L132\">dreaming.ts L132</a>、<a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/extensions/memory-core/src/dreaming.ts#L477\">trigger gate L477</a> · <a href=\"initiative-grok.html\">Grok initiative audit</a>。</p>\n<h3 id=\"5-自己選下一個任務不是新增喚醒種類\">5. 自己選下一個任務，不是新增喚醒種類</h3>\n<p>Voyager <code>learn()</code> 先取環境，再在迴圈中呼叫 <code>propose_next_task</code>、<code>rollout</code>，用結果更新歷史與 skill。一般 curriculum 路徑依 prompt 選題，有初始／背包條件等硬分支。價值在「如何選題並累積能力」，不是擺脫時間、事件與控制流。</p>\n<p>來源：<a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L295\">voyager.py L295</a> · <a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/curriculum.py#L240\">curriculum.py L240</a>。</p>\n<h3 id=\"6-更研究型的設計確實存在但要說出實際多了什麼\">6. 更研究型的設計確實存在，但要說出實際多了什麼</h3>\n<p>Generative Agents 的 reflection trigger 檢查累積重要性計數是否到門檻，再從記憶產生反思。這是執行中的條件判斷，可以抽象為狀態事件；不靠外部新訊息，也不是無因的觸發。其小鎮社交模擬與企業工作交付是不同驗證場景。</p>\n<p>來源：<a href=\"https://arxiv.org/abs/2304.03442\">作者論文</a> · <a href=\"https://github.com/joonspk-research/generative_agents/blob/fe05a71d3e4ed7d10bf68aa4eda6dd995ec070f4/reverie/backend_server/persona/cognitive_modules/reflect.py#L135\">reflect.py L135 固定版本</a>。</p>\n<p>MAGELLAN 在 online RL 中學習預測 competence／learning progress，用來選擇值得練習的目標。這確實比一次 prompt 判斷更複雜，但屬於學習政策，不是新的通知來源；本研究未重現論文實驗，也沒有證明它比簡單 scheduler 更適合催交檔案。<a href=\"https://arxiv.org/abs/2502.07709\">作者論文</a> · <a href=\"prioritization.html\">研究型排序比較</a>。</p>\n<h2 id=\"可靠性不是裝飾但也不是神祕的自主性\">可靠性不是裝飾，但也不是神祕的自主性</h2>\n<p>OpenBot <code>offerDueRoutines</code> 處理到期 occurrence、過期視窗及交付 queue；worker claim 確保多個程序不隨意重做同一工作。Grok 的 background wake 做 batching、ownership 與等待回接。這些工程影響是否能可靠長時間運作，值得比較；不應為了說明簡單，就把它們全部說成「只是 cron、沒有價值」。</p>\n<p>來源：<a href=\"https://github.com/CopilotKit/OpenBot/blob/7b94a0b802732e6491634160cf9ed3fcfb813424/server/src/routines/sweep.ts#L135\">OpenBot sweep.ts L135</a>、<a href=\"https://github.com/CopilotKit/OpenBot/blob/7b94a0b802732e6491634160cf9ed3fcfb813424/server/src/work/queue.ts#L268\">queue.ts L268</a> · <a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/background-wakes.ts#L359\">Grok background-wakes.ts L359</a>。</p>\n<h2 id=\"對本專案的選擇\">對本專案的選擇</h2>\n<p>先採「定時查看＋既有職責＋來源工具＋持久工作狀態＋模型判斷＋有根據的動作」。等實際問題出現再加：資料沒變但成本高 → change gate；時效不足 → event；事情做一半斷掉 → completion 接續；重複通知 → 去重與送達紀錄；找錯工作 → 改責任與判斷依據。</p>\n<p>「主動」和「互動像同事」是兩條研究主線。前者回答如何自行開始、推進；後者回答如何接話、打斷、交接、保持共同脈絡。見 <a href=\"../colleague-interaction.html\">同事互動研究</a>。</p>\n<p>驗證範圍：原始碼靜態呼叫路徑、官方文件及論文；沒有執行上游完整 runtime、雲端產品、Minecraft 或真人訊息 E2E。</p><details class=\"article-references\"><summary>本文參考來源 · 17</summary><p>以下保留本文實際引用的來源；引用不等於實測，適用範圍以正文說明為準。</p><ol><li><a href=\"https://github.com/NousResearch/hermes-agent/blob/fef0e16fe19b79ded929209f87c7434270b03825/cron/monitor.py#L125\">monitor.py L125</a></li><li><a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/src/cron/service/timer-outcomes.ts#L481\">OpenClaw timer-outcomes.ts L481</a></li><li><a href=\"https://github.com/NousResearch/hermes-agent/blob/fef0e16fe19b79ded929209f87c7434270b03825/hermes_cli/loops.py#L580\">Hermes loops.py L580</a></li><li><a href=\"https://code.claude.com/docs/en/scheduled-tasks\">Claude Code scheduled tasks</a></li><li><a href=\"https://github.com/openai/codex/blob/634ebc1865c6ac840ed3ba118f040d527bf4b55d/codex-rs/ext/goal/src/runtime.rs#L399\">Codex runtime.rs L399</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/completion-revivals.ts#L99\">Grok completion-revivals.ts L99</a></li><li><a href=\"https://code.claude.com/docs/en/goal\">Claude Code goal</a></li><li><a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/extensions/memory-core/src/dreaming.ts#L132\">dreaming.ts L132</a></li><li><a href=\"https://github.com/openclaw/openclaw/blob/91ea838947d30a65f1299b05fa42071917f2a293/extensions/memory-core/src/dreaming.ts#L477\">trigger gate L477</a></li><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/voyager.py#L295\">voyager.py L295</a></li><li><a href=\"https://github.com/MineDojo/Voyager/blob/55e45a880755d0c8c66ca7fb5fe7962ac8974f89/voyager/agents/curriculum.py#L240\">curriculum.py L240</a></li><li><a href=\"https://arxiv.org/abs/2304.03442\">作者論文</a></li><li><a href=\"https://github.com/joonspk-research/generative_agents/blob/fe05a71d3e4ed7d10bf68aa4eda6dd995ec070f4/reverie/backend_server/persona/cognitive_modules/reflect.py#L135\">reflect.py L135 固定版本</a></li><li><a href=\"https://arxiv.org/abs/2502.07709\">作者論文</a></li><li><a href=\"https://github.com/CopilotKit/OpenBot/blob/7b94a0b802732e6491634160cf9ed3fcfb813424/server/src/routines/sweep.ts#L135\">OpenBot sweep.ts L135</a></li><li><a href=\"https://github.com/CopilotKit/OpenBot/blob/7b94a0b802732e6491634160cf9ed3fcfb813424/server/src/work/queue.ts#L268\">queue.ts L268</a></li><li><a href=\"https://github.com/b-nnett/grok-bot-0.18-reconstructed/blob/a9f633e09d49a85829b8236331b9e21f7e612634/source/host/extensions/transcript/background-wakes.ts#L359\">Grok background-wakes.ts L359</a></li></ol></details>"};
