# Flows

End-to-end workflows that cut across phases. Each flow is one SVG + one short markdown
explaining the steps, actors, and where human-in-the-loop gates live.

Planned:

- `contract-review.svg` — legal contract review end-to-end (Phase 1)
- `agent-collaboration.svg` — how colleagues hand work to each other
- `auth-and-session.svg` — identity, login, session lifecycle (Phase 2+)
- `channel-routing.svg` — how the same colleague is reached from Slack / Linear / Claw3D (Phase 3)
